Problems 225
(e) Take the inverse Fourier transform ofG(ϱ , θ) to ﬁnd an expression for
g(ℓ, θ).
(f) Sketch g(ℓ,3 0◦) (include axis labels) and sketch its backprojection
image b30◦ (x, y).
6.9 Prove the convolution property of the radon transform:
R
{
f ∗ h)
}
= R{f }∗ R{h}.
CT Reconstruction
6.10 The ‘‘unit square function’’ is given by
s(x, y) =
{ 1 −1 ≤ x ≤ 1, −1 ≤ y ≤ 1
0 otherwise .
Let gs(ℓ, θ) be the 2-D Radon transform of s(x, y).
(a) Show that gs(ℓ, θ + π/2) = gs(ℓ, θ).
(b) Show that gs(ℓ, −θ) = gs(ℓ, θ).
(c) Write an expression for gs(ℓ, θ), −∞ <ℓ< +∞,0 ≤ θ<π given
knowledge of gs(ℓ, θ) only in the range −∞ <ℓ< +∞,0 ≤ θ<π / 4.
(d) Sketch gs(ℓ, θ)a t θ = 0, θ = π/8, and θ = π/4.
(e) Determine an expression for gs(ℓ, θ), 0 ≤ θ<π / 4.
6.11 The mass of an object is deﬁned as
m =
∫ ∞
−∞
∫ ∞
−∞
f (x, y) dxdy
and the center-of-mass is deﬁned as c = (cx, cy) where
cx = 1
m
∫ ∞
−∞
∫ ∞
−∞
xf (x, y) dxdy ,
cy = 1
m
∫ ∞
−∞
∫ ∞
−∞
yf (x, y) dxdy .
The mass of a projection is deﬁned as
mp(θ) =
∫ ∞
−∞
g(ℓ, θ) dℓ
and its center-of-mass is given by
cp(θ) = 1
mp(θ)
∫ ∞
−∞
ℓg(ℓ, θ) dℓ.
(a) Show that mp(θ) = m.
(b) Show that cp(θ) = cx cos θ + cy sin θ.